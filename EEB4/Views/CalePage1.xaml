<Page
    x:Class="EEB4.CalePage1"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:EEB4"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Page.Resources>
        <Style TargetType="CalendarViewDayItem">
            <Setter Property="Padding" Value="10,5,0,0"/>
        </Style>
    </Page.Resources>

    <Grid Background="{x:Null}">
        <Grid Name="view1" Margin="10" HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
            <CalendarView HorizontalAlignment="Stretch" DayOfWeekFormat="{}{dayofweek.abbreviated(7)}" VerticalAlignment="Stretch" HorizontalDayItemAlignment="Left" VerticalDayItemAlignment="Top" Background="{x:Null}" OutOfScopeBackground="#20FFFFFF" CalendarItemBackground="#a0fcfcfc" BorderBrush="{x:Null}" CalendarViewDayItemChanging="add_event"/>
        </Grid>
    </Grid>
</Page>
